# 🚀 Полная инструкция по деплою на сервер 80.66.64.9

## 📋 Вариант 1: Деплой через Git (Рекомендуется)

### ЭТАП 1: Первоначальная настройка сервера

#### 1.1 Подключитесь к серверу
```bash
ssh root@80.66.64.9
```

#### 1.2 Установите необходимые пакеты
```bash
# Обновляем систему
apt update && apt upgrade -y

# Устанавливаем Nginx, Git, Node.js
apt install nginx git nodejs npm -y

# Устанавливаем Certbot для SSL
apt install certbot python3-certbot-nginx -y

# Включаем Nginx
systemctl enable nginx
systemctl start nginx
```

#### 1.3 Клонируйте репозиторий и соберите проект
```bash
# Переходим в директорию для сайтов
cd /var/www

# Клонируем репозиторий
git clone https://github.com/ExecuteTyT/IslamPM.git islam-pm.dev

# Переходим в папку проекта
cd islam-pm.dev

# Устанавливаем зависимости
npm install

# Собираем проект
npm run build

# Копируем собранные файлы в корень (для Nginx)
cp -r dist/* ./

# Можно удалить папку dist
# rm -rf dist

# Устанавливаем права доступа
chown -R www-data:www-data /var/www/islam-pm.dev
chmod -R 755 /var/www/islam-pm.dev
```

#### 1.4 Создайте конфигурацию Nginx
```bash
nano /etc/nginx/sites-available/islam-pm.dev
```

**Вставьте следующую конфигурацию:**
```nginx
server {
    listen 80;
    server_name islam-pm.dev www.islam-pm.dev;

    root /var/www/islam-pm.dev;
    index index.html;

    # Блокируем индексацию поисковыми системами
    add_header X-Robots-Tag "noindex, nofollow, noarchive, nosnippet" always;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # robots.txt
    location = /robots.txt {
        add_header Content-Type text/plain;
    }

    # Кэширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|pdf)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header X-Robots-Tag "noindex, nofollow" always;
    }

    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
}
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

#### 1.5 Активируйте конфигурацию
```bash
# Создаем символическую ссылку
ln -s /etc/nginx/sites-available/islam-pm.dev /etc/nginx/sites-enabled/

# Проверяем конфигурацию
nginx -t

# Перезагружаем Nginx
systemctl reload nginx
```

#### 1.6 Настройка Firewall
```bash
# Разрешаем HTTP и HTTPS
ufw allow 'Nginx Full'
ufw status
```

#### 1.7 Установите SSL сертификат
```bash
certbot --nginx -d islam-pm.dev -d www.islam-pm.dev
```

**Следуйте инструкциям:**
1. Введите email для уведомлений
2. Согласитесь с условиями (Y)
3. Подписка на новости (N)
4. Выберите опцию **2** (redirect HTTP -> HTTPS)

---

### ЭТАП 2: Обновление сайта в будущем

#### Способ A: Вручную на сервере
```bash
ssh root@80.66.64.9

cd /var/www/islam-pm.dev

# Получаем последние изменения
git pull origin main

# Устанавливаем зависимости (если изменились)
npm install

# Собираем проект
npm run build

# Копируем новые файлы
cp -r dist/* ./

# Перезагружаем Nginx
systemctl reload nginx
```

#### Способ B: Через скрипт на сервере
```bash
ssh root@80.66.64.9

# Запускаем скрипт обновления
bash /var/www/islam-pm.dev/server-deploy.sh
```

#### Способ C: Удаленно с вашего компьютера
```powershell
# С вашего компьютера
ssh root@80.66.64.9 "bash /var/www/islam-pm.dev/server-deploy.sh"
```

---

## 📋 Вариант 2: Деплой через локальный build

### ЭТАП 1: Настройка сервера (без Git)

Выполните шаги 1.1, 1.2 (без git, nodejs, npm), 1.4, 1.5, 1.6, 1.7 из Варианта 1, но:

```bash
# Просто создайте пустую директорию
mkdir -p /var/www/islam-pm.dev
chown -R www-data:www-data /var/www/islam-pm.dev
chmod -R 755 /var/www/islam-pm.dev
```

### ЭТАП 2: Деплой с локального компьютера

#### На вашем компьютере (Windows):
```powershell
cd D:\VS\Resume\IslamPM

# Соберите проект
npm run build

# Загрузите файлы на сервер
scp -r dist\* root@80.66.64.9:/var/www/islam-pm.dev/
```

#### Или используйте автоматический скрипт:
```powershell
.\deploy.ps1
```

---

## 🔍 Проверка работы

### Откройте в браузере:
```
https://islam-pm.dev
```

### Проверьте статус на сервере:
```bash
# Статус Nginx
systemctl status nginx

# Проверьте файлы
ls -la /var/www/islam-pm.dev/

# Логи (если есть проблемы)
tail -f /var/log/nginx/error.log
```

---

## 🎯 Рекомендация

**Для первого деплоя:** Используйте **Вариант 1** (через Git)

**Преимущества:**
- ✅ Легко обновлять (`git pull`)
- ✅ Контроль версий на сервере
- ✅ Можно быстро откатиться к предыдущей версии
- ✅ Один скрипт для обновления

**Для быстрых обновлений:** Используйте **Вариант 2** (локальный build + scp)

**Преимущества:**
- ✅ Быстрее (не нужно собирать на сервере)
- ✅ Меньше нагрузка на сервер
- ✅ Не нужен Node.js на сервере

---

## 🛠️ Полезные команды

### Git операции на сервере
```bash
# Посмотреть текущую версию
git log -1 --oneline

# Откатиться к предыдущей версии
git reset --hard HEAD~1

# Обновить до конкретного коммита
git checkout <commit-hash>
```

### Nginx команды
```bash
# Проверить конфигурацию
nginx -t

# Перезагрузить
systemctl reload nginx

# Перезапустить
systemctl restart nginx

# Статус
systemctl status nginx
```

### SSL команды
```bash
# Проверить сертификаты
certbot certificates

# Обновить вручную
certbot renew

# Тестовое обновление
certbot renew --dry-run
```

---

## 🔒 Защита от индексации

Ваш сайт защищен от поисковых систем:
- ✅ `robots.txt` - блокирует всех ботов
- ✅ Meta-теги `noindex, nofollow` в HTML
- ✅ HTTP заголовки `X-Robots-Tag` в Nginx

**Текущие работодатели не найдут сайт через поиск!** 🛡️

---

## ✅ Итоговый чеклист

- [ ] Сервер настроен (Nginx, SSL)
- [ ] Проект развернут (Git или SCP)
- [ ] Конфигурация Nginx создана
- [ ] SSL сертификат установлен
- [ ] Сайт открывается на https://islam-pm.dev
- [ ] robots.txt блокирует индексацию

---

## 🎉 Готово!

Ваш сайт доступен по адресу: **https://islam-pm.dev** 🚀

